3984. Divisible Game — players alternate subtracting multiples of a number from N, last move wins. Sprague-Grundy / memoized recursion on N. ***

3983. Subsequence After One Replacement — replace at most one character in s, then check if s is a subsequence of t. Two-pointer with one-skip tolerance on s.

3980. Minimum Operations to Transform Binary String — match s1 to s2 using: single 0→1, pair 11→00 adjacent. Scan left-to-right; for each surplus (s1[i]=1, s2[i]=0), pair with i+1 using op 2. Cost = 1 if i+1 also surplus, 2 if i+1 is match (swing 0→0 or 1→1), 3 if i+1 is deficit. After pairing, surplus costs include fixing the neighbor back to its target. Impossible if surplus at last position (no i+1). DP with carry isn't sufficient because temporary breaks at position i (op 2 where s2[i]=1) can be optimal, requiring lookahead. Alternative: two-pointer match of adjacent surplus/deficit pairs in O(n). ***

3979. Maximum Valid Pair Sum — pick two non-overlapping pairs (i,j) and (k,l) with i<j<k<l maximizing sum(arr[i]+arr[j]+arr[k]+arr[l]) under a min-gap constraint between j and k. Precompute bestPairPrefix[i] = max pair sum in [0..i] (best adjacent pair in prefix), then for each j (as end of first pair), scan k<j+gap as start of second pair, combining with best pair from prefix before k. Alternatively, suffix max array for the second pair and combine with first pair from prefix — O(n).

3976. Maximum Subarray Sum After Multiplier — pick exactly one contiguous subarray, either multiply every element by k or divide by k, maximize total sum. 4-state Kadane: s0 (before op), s1 (in multiply subarray), s2 (in divide subarray), s3 (after op). s0 classical, s1/s2 start or continue the operation, s3 picks up after operation ends. O(n).
***

3975. Filter Occupied Intervals — two interval sets: main (to filter) and query (occupied regions). Remove any main interval overlapping a query. Sweep line: sort all (pos, delta, type) events. Track activeQueries count and activeMainIds set. When query starts (0→1), mark all activeMainIds as occupied. Main kept only if never marked. O((n+m)log(n+m)). Alternative: merge queries into disjoint spans, then check each main against them.

3974. Maximum Total Sum of K Selected Elements — pick exactly k elements, process in any order. Each step: either add value, or multiply by current mul (decrements after each op). Sort descending, take k largest, process each with mul * max(1, mul--). Greedy: largest elements get highest multipliers. O(n log n), no heap needed.

3970. Shortest Path With At Most K Consecutive Identical Characters — weighted graph, bound on consecutive edges of same label. Dijkstra with state (node, runLength). Edge weight determines shortest path, BFS insufficient.

3969. Valid Subarrays With Matching Sum Digits I — count subarrays where sum's first and last digits equal x. n ≤ 1500, O(n²) enumeration: for each l, accumulate sum r→r+1, check s%10==x && firstDigit(s)==x.

3968. Maximum Manhattan Distance After All Moves — moves string: U/D/L/R and _ (any direction). Count net x, net y, and wildcards z. Answer = |x| + |y| + z — each wildcard adds 1 by moving away from origin. Greedy, O(n).

3965. Finish Time of Tasks I — given task dependencies and durations, compute earliest finish. Topological sort (Kahn's algorithm). ***

3964. Minimum Lights to Illuminate a Road — points on a line, lights with range R. Each point at position p forms interval [p-R, p+R] where a light covers it. Greedy: sort all needed intervals by right endpoint, place light at that endpoint, skip all intervals it covers. Classic interval stabbing, O(n log n).

3961. Maximize Sum of Device Ratings — pick k devices with ratings dependent on selection order. Greedy with max-heap by rating ratio.

3960. Frequency Balance Subarray — longest subarray where each distinct value appears either f or 2*f times (both present). n ≤ 10³, O(n²): for each l, expand r right, maintain cnt (value→freq) and freq (freq→count). Condition: len(cnt)==1 or (len(freq)==2 and one freq doubles the other).

3955. Valid Binary Strings With Cost Limit — generate binary strings under cost constraint. Backtracking with cost-based pruning.

3952. Maximum Total Value of Covered Indices — tokens at positions where s[i]='1'. Each moves left by 1 at most, or stays. Maximize sum of nums at indices that end up with a token. DP with 1-bit state: dp(i, prevCovered). When prevCovered, token at i forced to stay (i-1 blocked). When not, choose between move left (cover i-1) or stay (cover i). s[i]='0' → skip. O(n), O(1).

3951. Minimum Energy to Maintain Brightness — n bulbs in line, each on covers self + adjacent (max 3 positions). Need total illumination >= brightness during given intervals. Sort intervals, merge overlapping, for each merged interval add ceil(brightness/3) * length. Sort+merge, O(n log n).

3947. Maximum Number of Items From Sale II — buy k items with budget, discounts applied after purchase affecting subsequent prices. Heap simulation.
3946. Maximum Number of Items From Sale I — buy k items with budget, simple discount model. Sort by effective price + greedy.
3942. Minimum Operations to Sort a Permutation — min adjacent swaps to sort. Inversion count (Fenwick tree) or cycle decomposition.
3941. Password Strength — count passwords meeting character class requirements in given range. Inclusion-exclusion / combinatorics.
3938. Maximum Path Intersection Sum in a Grid — two paths top-left to bottom-right, maximize sum of cells on both paths. DP on diagonals (3D DP: r1+r2 = step).
3937. Minimum Operations to Make Array Modulo Alternating I — make array alternate high/low under modulo. Try both patterns (peak/valley), count mismatches.
3933. Largest Local Values in a Matrix II — for each cell, max in surrounding (2k+1)×(2k+1) region. 2D sliding window / prefix max per row.
3932. Count K-th Roots in a Range — count numbers in [L,R] where floor(k-th root) is integer. Binary search on root value.
3927. Minimize Array Sum Using Divisible Replacements — replace elements with their divisors to minimize sum. Greedy with max-heap (replace largest first).
3926. Count Valid Word Occurrences — count words matching pattern in text stream. Rolling hash (Rabin-Karp) or prefix trie.
3923. Minimum Generations to Target Point — cellular automaton doubling from origin to reach target coordinate. BFS level-order (layer by layer expansion from origin, stop when reaching target).
3922. Minimum Flips to Make Binary String Coherent — min flips so no two adjacent bits equal. Try both patterns (0101... and 1010...), take min.
3919. Minimum Cost to Move Between Indices — cost = |value diff|, find min cost path through array. Prefix-suffix minimum arrays.
3918. Sum of Primes Between Number and Its Reverse — sum all primes in range [n, reverse(n)]. Sieve of Eratosthenes + prefix sum.
3914. Minimum Operations to Make Array Non Decreasing — increment elements to make array non-decreasing. Greedy: carry forward required adjustment.
3913. Sort Vowels by Frequency — sort vowels in string by frequency descending, keep consonants in place. Counting sort by frequency.
3909. Compare Sums of Bitonic Parts — split array into bitonic segments (↑ then ↓), compare sums. Two-pointer peak detection.
3905. Multi Source Flood Fill — flood fill from multiple starting points simultaneously. Multi-source BFS (push all sources, layer by layer).
3904. Smallest Stable Index II — find leftmost index where prefix condition holds. Prefix sum + binary search on prefix property.
3900. Longest Balanced Substring After One Swap — binary string, swap any two bits, find longest balanced (equal zeros and ones). Prefix sum + hash map of first occurrence.
3899. Angles of a Triangle — given three points (angles on a circle), compute triangle angles sorted. Math: atan2 for angle between vectors.
3896. Minimum Operations to Transform Array Into Alternating Prime — make array alternate prime/composite. Binary search on nearest prime (precompute primes via sieve).
3895. Count Digit Appearances — count how many times digit d appears in range [1, n]. Digit DP (state: pos, leadingZero, tight, countSoFar).
3891. Minimum Increase to Maximize Special Indices — increment elements to maximize count of indices meeting condition. Knapsack-like DP on sorted values.
3890. Integers With Multiple Sum of Two Cubes — find numbers expressible as sum of two cubes in more than one way. Precompute all sums ≤ bound into hash map.
3889. Mirror Frequency Distance — distance metric between frequency maps of two strings. Counting sort + absolute difference per character.
3885. Design Event Manager — schedule events with start/end, support overlap queries. Balanced BST (TreeMap / sorted container) for interval management.
3882. Minimum XOR Path in a Grid — grid, min XOR of path values top-left to bottom-right. DP with binary trie per cell for prefix XOR lookup.
3881. Direction Assignments With Exactly K Visible People — arrange people in a line so exactly K can see forward. Combinatorics DP (Eulerian-ish: dp[n][k] for visible count).
3877. Minimum Removals to Achieve Target XOR — remove min elements so remaining XOR ≤ target. DP on XOR state space (knapsack on XOR values).
3876. Construct Uniform Parity Array II — build array meeting sum and parity constraints. Greedy construction: place numbers to meet sum constraint while maintaining parity alternation.
3872. Longest Arithmetic Sequence After Changing At Most One Element — longest arithmetic subsequence allowing one skip. DP with difference map per index.
3871. Count Commas in Range II — count comma characters when writing numbers in range with digit grouping (e.g., 1,000). Digit DP.
3868. Minimum Cost to Equalize Arrays Using Swaps — swap elements between two arrays to make them match, minimize cost. Greedy cross-matching.
3867. Sum of GCD of Formed Pairs — partition array into pairs to maximize sum of GCDs. Sort descending + greedy adjacent pairing.
3863. Minimum Operations to Sort a String — min adjacent swaps to make string characters sorted by ASCII. Inversion count on character positions (Fenwick tree).
3862. Find the Smallest Balanced Index — smallest index where prefix and suffix have equal property. Prefix + suffix precompute arrays.
3858. Minimum Bitwise OR From Grid — find path top-left to bottom-right minimizing OR of visited cells. DP with bitmask states (2D DP tracking reachable OR values).
3857. Minimum Cost to Split Into Ones — split binary string into substrings of all ones, each split has cost. Linear DP (dp[i] = min cost to process prefix i).
3854. Minimum Operations to Make Array Parity Alternating — adjacent swaps to make array alternate even/odd. Count parity inversions from target pattern.
3853. Merge Close Characters — merge adjacent same characters in string, count resulting length. Stack simulation (push, pop when adjacent matches).
3849. Maximum Bitwise XOR After Rearrangement — rearrange array to maximize XOR of adjacent pair results. Greedy bit-by-bit (trie or most-significant-bit prioritization).
3848. Check Digitorial Permutation — verify if a number satisfies a specific digit permutation property. Math: digit extraction + factorial verification.
3847. Find the Score Difference in a Game — two players pick values from array ends, compute optimal score difference. Minimax DP (dp[l][r] = best score on subarray).
3844. Longest Almost-Palindromic Substring — longest substring that is palindrome when at most one character is changed. Expand around center with one-skip tolerance.
3843. First Element With Unique Frequency — find first element whose frequency in the array is unique. Frequency map + set of seen frequencies.
3840. House Robber V — rob non-adjacent nodes on a tree with weighted values. Tree DP (dp[node][0/1] = max value robbing/not robbing this node).
3839. Number of Prefix Connected Groups — count connected components as nodes are added one by one. DSU (Union-Find) with incremental edge addition.
3835. Count Subarrays With Cost ≤ K — count subarrays where max-min ≤ K. Monotonic deque for sliding range min/max + two-pointer.
3834. Merge Adjacent Equal Elements — repeatedly merge adjacent equal values (like 2048). Stack simulation (push, compare top, merge/keep).
3829. Design Ride Sharing System — match riders with drivers in real time, geospatial. QuadTree / grid partitioning + priority queue.
3828. Final Element After Subarray Deletions — repeatedly delete a subarray meeting condition until one element remains. Simulation with modulo arithmetic to detect cycle/remainder.
3825. Longest Strictly Increasing Subsequence With Non-Zero Bitwise AND — LIS where adjacent elements must have non-zero bitwise AND. DP with bitmask tracking (dp[mask] = LIS length ending with mask bits).
3824. Minimum K to Reduce Array Within Limit — find min K such that reducing by K per operation makes all elements ≤ limit. Binary search on K.
3820. Pythagorean Distance Nodes in a Tree — count node pairs in tree where Euclidean distance between node values satisfies sqrt property. Tree DFS + hash map of value frequencies.
3819. Rotate Non Negative Elements — rotate only non-negative elements of array, leave negatives fixed. Two-pass: extract non-negatives, rotate, place back.
3818. Minimum Prefix Removal to Make Array Strictly Increasing — remove shortest prefix so remaining is strictly increasing. Reverse scan: find longest suffix that's strictly increasing.
3815. Design Auction System — bid/ask matching with price-time priority. Two heaps (min-heap for asks, max-heap for bids, like order book).
3814. Maximum Capacity Within Budget — maximize total capacity choosing items within budget. Greedy: sort by cost-to-capacity ratio, pick cheapest first.
3811. Number of Alternating XOR Partitions — partition array so adjacent segments alternate XOR parity. DP on prefix XOR states.
3810. Minimum Operations to Reach Target Array — start from zeros, increment ranges or single elements to reach target. Difference array / sweep line.
3809. Best Reachable Tower — tower defense: place towers maximizing coverage with overlap constraints. Geometry sweep line for maximum coverage under radius constraints.
3808. Find Emotionally Consistent Users — users with consistent sentiment across time windows. Hash map of sentiment flip tracking.
3805. Count Caesar Cipher Pairs — count pairs of strings that are Caesar shifts of each other. Hash map with normalized form (shift difference).
3804. Number of Centered Subarrays — subarrays where middle element is the average of the rest. Prefix sum + enumeration at each position.
3800. Minimum Cost to Make Two Binary Strings Equal — flip bits with position-dependent cost to make strings equal. State machine DP (dp[i][carry/flip-state]) tracking pending cost across positions.
3799. Word Squares II — find all word squares given a dictionary. Backtracking with prefix trie for pruning invalid partial squares.
3796. Find Maximum Value in a Constrained Sequence — sliding window max with additional value constraint outside the window. Monotonic deque.
3795. Minimum Subarray Length With Distinct Sum at Least K — shortest subarray where sum of distinct elements ≥ K. Sliding window + frequency map.
3790. Smallest All-Ones Multiple — smallest number consisting only of digit 1 that is divisible by N. BFS on remainder states (like 1 → 11 → 111).
3789. Minimum Cost to Acquire Required Items — buy items with bundle discounts, minimize cost to get all required items. Bitmask DP (subset DP similar to traveling salesman).
3788. Maximum Score of a Split — split array at one point, score = left sum × right max. Prefix sum + suffix max precompute.
3784. Minimum Deletion Cost to Make All Characters Equal — delete chars with given costs so remaining string has all same char. Try each character as the survivor, take min cost.
3781. Maximum Score After Binary Swaps — swap adjacent binary bits to maximize resulting integer value. Greedy: bubble 1s toward most significant positions.
3780. Maximum Sum of Three Numbers Divisible by Three — pick three numbers from array with sum divisible by 3, maximize sum. Greedy with top-3 buckets per remainder.
3779. Minimum Number of Operations to Have Distinct Elements — make all array elements distinct with min operations. Hash map + greedy reassignment.
3776. Minimum Moves to Balance Circular Array — distribute values around circular array to equalize, min adjacent transfers. Greedy: compute prefix deviation from average.
3775. Reverse Words With Same Vowel Count — reverse word pairs in string where each pair has same vowel count. Two-pointer on vowel counts.
3771. Total Score of Dungeon Runs — minimum initial health needed to complete dungeon path top-left to bottom-right. Binary search on initial health + DP feasibility.
3770. Largest Prime from Consecutive Prime Sum — longest prime expressible as sum of consecutive primes. Sieve + prefix sum + two-pointer.
3767. Maximize Points After Choosing K Tasks — pick k tasks within budget, each with skill requirement, maximize total points. Greedy with max-heap (sort by requirement, offer to heap).
3766. Minimum Operations to Make Binary Palindrome — min flips to make binary string palindrome. Two-pointer comparison counting mismatches.
3765. Complete Prime Number — find special prime numbers meeting complete property. Enumeration / sieve + digit check.
